# CAC_Part_1

航空大学校入学試験「総合Part I」思考パズル問題作成プロジェクト

## 概要

高度な論理的思考力を測定する思考パズル問題を作成するプロジェクト。
Python検証により解の一意性を保証する。

## 問題仕様

### 作成ルール
- **2問1セット**: 標準難度（問1）と高難度（問2）をペアで作成
- **出力形式**: 2問1セットで1つのHTMLファイルを生成
- **ファイル命名**: `航大思考{N}.html`（Nはセット番号: 1, 2, 3, ...）
  - 例: 航大思考1.html（問1・問2）、航大思考2.html（問1・問2）...

### 難易度別仕様

| 項目 | 問題1（標準） | 問題2（高難度） |
|------|-------------|----------------|
| 配点 | 6点 | 6点 |
| 制限時間 | 3分 | 5分 |
| 難易度 | ★★★★★ | ★★★★★★★★ |
| 解答時間目安 | 170秒 | 310秒 |

## 測定能力

- 論理推論
- 空間視覚化
- パターン認識
- 数学的思考
- 制約充足
- 資料読み取り

## ファイル構成

```
航大思考{N}.html - 問題出力先（2問1セットで生成）
CLAUDE.md        - Claude Code用ガイドライン
README.md        - プロジェクト説明（このファイル）
難易度基準.md    - 難易度基準の参考資料
template.html    - HTMLテンプレート
```

## 作成プロセス

### 1. 問題設計

問題タイプを選択:
- 制約充足
- 暗号解読
- 空間推論
- パターン認識
- データ分析

### 2. Python検証（必須）

```python
def verify_solution():
    """全ての可能性を検証し、唯一解を確認"""
    valid_solutions = []
    # 全パターンを総当たり
    # 制約条件を適用
    assert len(valid_solutions) == 1, f"解が{len(valid_solutions)}個存在"
    return valid_solutions[0]
```

### 3. 正解番号のランダム配置

```bash
python3 -c "import random; print(random.randint(1, 5))"
```

- 各問題ごとに実行
- 手動での番号決定は禁止

### 4. HTML作成

- `template.html` の構造・スタイルを一切変更しない
- 編集範囲: `<!-- ここから問題セクション -->` 〜 `<!-- 解答・解説セクション -->` のみ
- カラー: 白黒（グレースケール可）
- **出力形式**: 2問1セットで `航大思考{N}.html` として生成
  - 各ファイルに問1（標準）と問2（高難度）を含める

## SVG図表作成

座標系を明示的に文書化する:

```html
<!--
座標系定義:
- 原点: (50, 250)
- X軸: 右方向, 1単位=30px
- Y軸: 上方向, 1単位=40px
-->
```

作成後はPythonで座標を検証する。

## 品質チェックリスト

- [ ] Python検証で解が唯一
- [ ] すべての条件が解導出に必要
- [ ] 矛盾する条件が存在しない
- [ ] SVG座標が意図した位置と一致
- [ ] 正解番号がランダム化されている
- [ ] 複数ステップの推論が必要

## 禁止事項

- 視覚的矛盾
- 自明な解
- 航空専門知識を必要とする設計
- Python検証の省略
- 正解番号の手動選択
